<h3>投稿一覧</h3>
<% @posts.each do |post| %>
  <%= post.user.name %>
  <%= post.title %>
  <p>カテゴリー：<%= post.category.name %></p>
    <p>ジャンル：
      <% post.genre_posts.each do |genre_post| %>
      <%= genre_post.genre.name %>
    <% end %>
    </p>
  <%= post.opinion %><br>
<% end %>